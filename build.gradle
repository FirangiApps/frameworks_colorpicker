buildscript {
    repositories {
        mavenCentral()
        jcenter()
        google()
    }
}
apply plugin: 'com.android.library'

android {
    sourceSets.main {
        manifest.srcFile 'AndroidManifest.xml'
        java.srcDirs = ['src']
        res.srcDirs = ['res']
    }
}
repositories {
    mavenCentral()
    jcenter()
    google()
}